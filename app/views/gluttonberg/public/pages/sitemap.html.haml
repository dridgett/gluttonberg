- s = SitemapGenerator::Interpreter.run
- Gluttonberg::Sitemap.links.each do |group , pages|
  %ul
    %li= group.humanize
    %li
      %ul
        - pages.each do |page|
          %li= link_to page[:title] , page[:path]