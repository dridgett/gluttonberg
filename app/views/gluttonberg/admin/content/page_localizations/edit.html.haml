- @page_title = "Edit localization"
= sub_nav do
  = back_link("Pages", slice_url(:pages))
= partial(:form, :action => slice_url(:page_localization, :page_id => params[:page_id], :id => @page_localization))