.secondary-navigation
  - sub_nav do
    = back_link("Library", admin_assets_url)

.clear
%h2.title
  Add New Collection
.form
  .center-new
    .block
      .content
        .inner
          %br
          - form_for( :collection , @collection , :url => url_opts ,  :html => { :method => method_name} ) do |collection_form|

            %h4 Name
            .fieldset
              = collection_form.text_field(:name)
  
            = form_controls(admin_assets_url)
